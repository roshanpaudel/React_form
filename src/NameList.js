import React from "react";

export const NameList = () => {
  return (
    <div>
      <ul>
        {/* {list.map((item, i) => {
          return <li key={i}>{item}</li>;
        })} */}
      </ul>
    </div>
  );
};
